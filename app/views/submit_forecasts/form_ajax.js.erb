<%# Change team name on form %>
document.querySelector("#forecast-title #team-name").innerText = "<%= team&.name&.titleize || "Team" %>"
<%# replace container with form relevant to team %>
document.getElementById("forecast-form-container").innerHTML = "<%= j render "form", team: team  %>"

console.log(window.location.pathname.split("/")[1] === "forecasts")

history.pushState(
  { 'team': '<%= team&.slug %>' },
  '',
  `/forecasts/${'<%= team&.slug %>'}`
)


